/**
 * Header Navigation Context for Metadata Remote
 * Handles keyboard navigation for header icons and controls
 */

(function() {
    // Create namespace if it doesn't exist
    window.MetadataRemote = window.MetadataRemote || {};
    window.MetadataRemote.Navigation = window.MetadataRemote.Navigation || {};
    
    // Create shortcuts
    const State = window.MetadataRemote.State;
    
    window.MetadataRemote.Navigation.HeaderNavigation = {
        /**
         * Initialize the module
         */
        init() {
        }
    };
})();